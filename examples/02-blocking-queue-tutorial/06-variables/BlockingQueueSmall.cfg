\* 手で数えられる範囲に絞った版。初期状態は (2^2-1) * (2^2-1) * 2 = 18 個。
CONSTANTS
    BufCapacity = 2
    Producers = {p1, p2}
    Consumers = {c1, c2}

INIT Init
NEXT Next

INVARIANT TypeOK
INVARIANT CapacityOK
INVARIANT NoDeadlock

CHECK_DEADLOCK FALSE
