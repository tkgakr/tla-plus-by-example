\* 構成は Init が選ぶ。CONSTANTS はその選択肢の上限を与えるだけである。
CONSTANTS
    BufCapacity = 3
    Producers = {p1, p2, p3, p4}
    Consumers = {c1, c2, c3}

INIT Init
NEXT Next

INVARIANT TypeOK
INVARIANT CapacityOK
INVARIANT NoDeadlock

\* デッドロックは NoDeadlock で検出するため、既定検査は切る。
CHECK_DEADLOCK FALSE
