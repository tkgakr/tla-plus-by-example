\* 比較用: NoDeadlock を外し、TLC の既定のデッドロック検出だけに任せる。
\* 03 章の BlockingQueue.cfg と同じ検査であり、報告のされ方だけが違う。
CONSTANTS
    BufCapacity = 1
    Producers = {p1}
    Consumers = {c1, c2}

INIT Init
NEXT Next

INVARIANT TypeOK
INVARIANT CapacityOK

CHECK_DEADLOCK TRUE
