\* p1c2b1: デッドロックを「不変条件 NoDeadlock の違反」として検出する。
CONSTANTS
    BufCapacity = 1
    Producers = {p1}
    Consumers = {c1, c2}

INIT Init
NEXT Next

INVARIANT TypeOK
INVARIANT CapacityOK
INVARIANT NoDeadlock

\* デッドロック検出は NoDeadlock が担うため、TLC の既定検査は切る。
CHECK_DEADLOCK FALSE
