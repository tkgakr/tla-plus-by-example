\* BlockingQueueUnsound.cfg から SYMMETRY 行だけを外した版。
\* 同じ NotSoleSecond が初期状態で違反として報告される。
CONSTANTS
    BufCapacity = 3
    Producers = {p1, p2, p3, p4}
    Consumers = {c1, c2, c3}

INIT Init
NEXT Next

INVARIANT NotSoleSecond

CHECK_DEADLOCK FALSE
