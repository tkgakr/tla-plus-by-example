\* 06 章と同じ上限に SYMMETRY を足しただけの構成。
CONSTANTS
    BufCapacity = 3
    Producers = {p1, p2, p3, p4}
    Consumers = {c1, c2, c3}

SYMMETRY Symmetry

INIT Init
NEXT Next

INVARIANT TypeOK
INVARIANT CapacityOK
INVARIANT NoDeadlock

\* デッドロックは NoDeadlock で検出するため、既定検査は切る。
CHECK_DEADLOCK FALSE
