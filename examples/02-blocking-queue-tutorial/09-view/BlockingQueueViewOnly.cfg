\* 比較用: VIEW だけを使い、SYMMETRY を外した版。
\* 2 つの削減がどれだけ独立に効くかを測る。
CONSTANTS
    BufCapacity = 3
    Producers = {p1, p2, p3, p4}
    Consumers = {c1, c2, c3}

VIEW View

INIT Init
NEXT Next

INVARIANT ShapeOK
INVARIANT CapacityOK
INVARIANT NoDeadlock

CHECK_DEADLOCK FALSE
