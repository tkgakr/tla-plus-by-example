\* BlockingQueueUnsound.cfg から VIEW 行だけを外した版。
\* 同じ NotThreeDistinct が違反として報告される。
CONSTANTS
    BufCapacity = 3
    Producers = {p1, p2, p3, p4}
    Consumers = {c1, c2, c3}

INIT Init
NEXT Next

INVARIANT NotThreeDistinct

CHECK_DEADLOCK FALSE
