\* VIEW と SYMMETRY の両方を使う。検査するのは buffer の中身を見ない性質だけ。
CONSTANTS
    BufCapacity = 3
    Producers = {p1, p2, p3, p4}
    Consumers = {c1, c2, c3}

SYMMETRY Symmetry
VIEW View

INIT Init
NEXT Next

INVARIANT ShapeOK
INVARIANT CapacityOK
INVARIANT NoDeadlock

CHECK_DEADLOCK FALSE
